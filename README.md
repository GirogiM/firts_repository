# Basic Web UI

This is a simple Node.js application demonstrating a basic web UI with registration, login and a protected homepage.

## Setup

Install dependencies with `npm install` and start the server with `npm start`.

The application stores users in memory and is not meant for production use.
